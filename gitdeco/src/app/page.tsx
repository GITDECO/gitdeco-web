"use client";

export default function Home() {
  return (
    <main className="">
      <div>hello</div>
    </main>
  );
}

// <>
//   {data.categories[0].cards.map(data:Card index) => { }}

//   <div>{index}</div>
//   <Cardnews key={index} data={data} />
// </>
